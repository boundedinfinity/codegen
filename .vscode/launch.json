{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "codegen codegen-spec.yaml",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}", 
            "args": ["${env:HOME}/codegen-project/spec.yaml"],
        }
    ]
}
